# coding: utf-8




for _ in range(N):  # _ 替代常用的i，j，k，GOOD
    self._single_refine()

np.array.flatten() # 一行接下一行，展成一个很长的行
np.nonzero(arr) # 返回arr中非零元的索引

numpy.union1d(ar1, ar2) # 两个数组的集合并

numpy.setdiff1d(ar1, ar2, assume_unique=False) # 在ar1不在ar2中的元素

numpy.ascontiguousarray() # Return a contiguous array in memory (C order).

numpy.tile(A, reps) # Construct an array by repeating A the number of times given by reps.











